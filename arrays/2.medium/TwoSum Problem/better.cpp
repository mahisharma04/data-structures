#include <iostream>
#include <map>
#include <vector>
using namespace std;

//the better solution uses hashing, we use hashMap to store what elements are present in the array, and check if the remainder element is also present.

